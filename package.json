{
  "name": "contact-email-marketing-platform",
  "version": "1.0.0",
  "description": "Plateforme complète de gestion des contacts et email marketing",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "setup": "npm install && cd server && npm install && cd ../client && npm install",
    "db:setup": "cd server && npm run db:setup"
  },
  "keywords": ["contacts", "email-marketing", "crm", "nodejs", "react", "mysql"],
  "author": "Golf Club CRM",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}